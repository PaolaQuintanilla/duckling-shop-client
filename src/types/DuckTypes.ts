export type Duck = {
  _id: string;
  color: string;
  size: string;
  price: number;
  quantity: number;
};

export type DuckInput = {
  color: string;
  size: string;
  price: number;
  quantity: number;
};
